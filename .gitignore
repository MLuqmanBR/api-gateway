node_modules/
dist/
server/data/
*.db
*.db-wal
*.db-shm
*.bak
.env
.env.local
.DS_Store

# Runtime artifacts
.api-gateway.pid
.api-gateway.instances
server.log
server.log.*

# Personal deployment scripts (contain keys/credentials — kept local)
deploy-pi.sh
update-hermes.sh

# Monetization planning — private, never committed
monetization/
# OMP agent-memory mount artifact — a directory literally named "memory:"
# (with the colon) at the repo root; local-only, never commit it.
memory:/
BUGFIX-REPORT-2026-06-25.md

# AI assistant artifacts (local only)
CLAUDE.md
omp-session-*
AUDIT*.md
PRIVACY-LAYER-FABLE.md
UPGRADE-PROGRESS.md
.upgrade-findings/
FEATURES-FABLE.md
PROJECT-STATE.md
temp/

# Python (native desktop app)
.venv/
__pycache__/
*.pyc
*.egg-info/
build/
.pytest_cache/
.claude/
